# config/__init__.py
# -----------------------------------------------------------------------------
# The presence of this file (even if empty) tells Python:
# "treat the 'config' folder as a PACKAGE, so other code can do `import config`."
#
# Every folder you want to import from in Python 3 should have one of these.
# It can be empty, or it can contain initialization code that runs when the
# package is first imported.
# -----------------------------------------------------------------------------
